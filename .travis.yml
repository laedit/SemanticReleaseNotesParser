dotnet: 1.0.3
language: csharp
dist: trusty

cache:
  directories:
  - src/packages
  - tools

before_script:
 - chmod +x build.sh

script:
  - ./build.sh

notifications:
  email:
    on_success: change
